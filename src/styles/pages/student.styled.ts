"use client";
import styled from "styled-components";
import { BaseMain } from "./generic.styled";

export const StudentMainContent = styled(BaseMain)`
  display: flex;
  flex-direction: column;
  background-color: white;
  gap: 1rem;
  padding: 0.75rem;
  border-radius: 1rem;
  height: fit-content;
`;
